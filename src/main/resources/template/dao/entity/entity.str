package ${system.package}.dao.entity;

import ${entity.import}

/**
 * ${entity.remarks}
 *
 * @author ${system.author}
 * @since ${system.date}
 */
public class ${entity.className} {

    ${repeat start}
    /**
     * ${entity.field.remarks}
     */
    private ${entity.field.javaType} ${entity.field.fieldName};

    ${repeat end}
}